management:
  endpoints:
    web:
      exposure:
        include: '*'
        
spring:
  application:
    name: api-gateway
  cloud:
    kubernetes:
      config:
        sources:
         - name: general
      reload:
        enabled: true
